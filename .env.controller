# Controller Service Configuration
# This environment file is for standalone Controller deployment

# Controller Server Settings
CONTROLLER_ADDR=:8080
DATABASE_PATH=/app/data/data.db
POLL_INTERVAL=5

# Admin Credentials
# ⚠️ IMPORTANT: Change these in production!
ADMIN_USER=admin
ADMIN_PASSWORD=changeme

# Agent Credentials  
# ⚠️ IMPORTANT: Change these in production!
# These must match the credentials configured in Agent deployments
AGENT_USER=agent
AGENT_PASSWORD=changeme

# Docker Compose Settings
COMPOSE_PROJECT_NAME=sdm-controller
