# Agent + Worker Combined Deployment Configuration
# This environment file is for deploying Agent and Worker together

# Controller Connection
# ⚠️ REQUIRED: Set this to your Controller's URL
CONTROLLER_URL=http://your-controller-host:8080

# Worker Service Settings
WORKER_ADDR=:8082
WORKER_URL=http://worker:8082

# Agent Service Settings
AGENT_ADDR=:8081
POLL_INTERVAL=5
REQUEST_TIMEOUT=10

# Agent Authentication
# ⚠️ IMPORTANT: These must match the credentials configured in the Controller
AGENT_USER=agent
AGENT_PASSWORD=changeme

# Agent Identification (optional)
AGENT_HOSTNAME=

# Agent Registration Retry Settings
REGISTRATION_MAX_RETRIES=5
REGISTRATION_INITIAL_BACKOFF=1
REGISTRATION_MAX_BACKOFF=30
REGISTRATION_BACKOFF_MULTIPLIER=2.0

# Docker Compose Settings
COMPOSE_PROJECT_NAME=sdm-agent-worker
